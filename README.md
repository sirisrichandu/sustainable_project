Helmet Detection System Using YOLO
📌 Project Description

This project detects whether a motorcycle rider is wearing a helmet or not using a deep learning-based object detection model (YOLO). The system processes images or video input and identifies helmet and non-helmet riders in real-time.

🛠 Technologies Used

Python

YOLO Algorithm

OpenCV

NumPy

TensorFlow / PyTorch

💻 System Requirements

Python 3.x

8GB RAM (Recommended)

Webcam (for live detection)

📂 Project Structure

dataset/

model/

detect.py

requirements.txt

README.md

▶ How to Run the Project

Install required libraries:

pip install -r requirements.txt


Run detection:

python detect.py


The system will display helmet detection results in a window.

📊 Output

The system displays bounding boxes with labels:

Helmet

No Helmet

📌 Future Improvements

Cloud deployment

Automatic fine generation

Integration with traffic cameras
